name: learn-github-actions
on: [push]
jobs:
  Write-to-Firebase-Database:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - uses: actions/setup-python@v2
        # with:
            # python-version: '3.10.1' # Version range or exact version of a Python version to use, using SemVer's version range syntax
            # architecture: 'x64' # optional x64 or x86. Defaults to x64 if not specified
            
            
      - uses: arduino/compile-sketches@v1
        with:
            fqbn: "esp32:esp32:ESP32 Dev Module"
            platforms: |
            - name: "esp32:esp32"
              source-url:|              
              "https://github.com/espressif/arduino-esp32/releases/download/1.0.6/esp32-1.0.6.zip"
            
                
        
              
            
      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v1      
      
      # - name: Decrypt GPG file
        # run: |
          # gpg --quiet --batch --yes --decrypt --passphrase="${{ secrets.GPG_PASSPHRASE }}" --output $GITHUB_WORKSPACE/Private-key.json firebase-key.json.gpg     
        # env:
            # GPG_PASSPHRASE: ${{secrets.GPG_PASSPHRASE}}
            
      # - name: Upgrade pip and install Firebase Admin SDK
        # run: python -m pip install --upgrade pip
             # pip install --upgrade firebase_admin                      
      
      # - name: Firebase - Python function
        # run: python firebase_write.py        

      